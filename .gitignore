# --- Virtual environment ---
.venv/
venv/
env/

# --- Python caches ---
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Market data: NEVER commit (yfinance pulls Yahoo data; licensing) ---
data/
data_cache/
*.parquet

# --- Generated analysis artifacts (re-creatable via the pipeline) ---
# The whole output/ folder is regenerable and git-ignored. The key figures the
# README/STUDY_SUMMARY reference are copied into assets/ (which IS tracked).
output/

# --- XSMOM quarantine (separated cross-sectional-momentum research) ---
# Held on disk for reference but kept OUT of this clean TSMOM repo.
_quarantine_xsmom/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- NOTE: assets/ is intentionally NOT ignored (tracked figures for the README) ---

# --- Credentials: NEVER commit ---
# Added 2026-09-17 at the CTA-EDGE-04-MMV data freeze. The MMV lineage needs a
# FRED/ALFRED API key on this machine; nothing in this file previously stopped a
# `git add -A` from committing one. These patterns are preventive, not reactive:
# no credential has ever been committed to this repository.
.env
.env.*
!.env.example
*.env
secrets.json
.secrets
config.local.py
*.key
*.pem
.fredapikey
.fred_api_key
.alfredapikey
