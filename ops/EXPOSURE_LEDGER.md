# TSMOM-EXT-001 — EXPOSURE LEDGER (research axis)

Canonical, append-only **research-axis** exposure record for the TSMOM extension
program, per the ratified L6 owner decision §10.1 in
`Quant trade/L6_RATIFICATION_AND_OWNER_DECISIONS_2026-08-21.md`
(SHA-256 `5E465D14B4A0F83BF7856A1875E7E502F3D76AA17AAD284B55CA221000423ACF`),
and per `research/extensions/TSMOM_EXTENSION_RESEARCH_PROGRAM_v2.md` §0B.

**Created:** 2026-09-07T20:20Z (Wave 0 governance execution, authorised by Aaron)
**Record type:** `RECORD_TYPE=EXPOSURE_LEDGER` · axis `RESEARCH`
**Append-only.** Rows are never edited, deleted or reordered. An erratum appends
a new row citing the row it corrects. `qros-state.yaml` may cache this file in
`cache.exposure_mirror`; **this file, not the cache, is authoritative.**

**File shape is load-bearing.** §10.1 fixes the event table's columns at
`| ts | scope | classification | granularity | artifact_or_pointer | note |`,
and the runtime parses the **first** markdown table in this file as that table
and every later `|…|` line as a row. This file therefore contains **exactly one
markdown table** — the event table, last — and every other structure here is
prose or a list. Adding a second table breaks machine parsing of the ledger.

**Seat exposure does not belong here.** Reviewer-seat exposure is recorded in
`ops/REVIEWER_EXPOSURE_LOG.md`. Burning a seat consumes no research degrees of
freedom, and a seat event never enters this ledger. The seat cross-reference
below carries **no classification and no scope**, so no linkage can be mistaken
for a research exposure event.

---

## Header block — normalization rule

NORMALIZATION_RULE: CLASSIFICATION_COLUMN_IS_CANONICAL

The `classification` column carries a ratified `TBL-EXPOSURE-CLASSES` token
directly, and that table alone fixes each token's contribution to the normalized
value. No project-local remapping is declared, and none is intended:
`NO_OUTCOME` and `GENERATED_NOT_SEEN` contribute `NONE`, `REVEALED_AGGREGATE`
contributes `AGGREGATE`, and `REVEALED_TARGET_METRIC` contributes
`TARGET_METRIC`.

The normalized value for a scope is the **maximum** contribution over the rows
falling in that scope, on the fixed ordering `NONE < AGGREGATE < TARGET_METRIC`.
A classification name is never itself an exposure value.

**A scope containing no rows normalizes to `UNKNOWN`, never to `NONE`.** §10.1,
verbatim: *"Absence of a ledger or of a rule renders `OUTCOME_EXPOSURE=UNKNOWN`,
never `NONE`."*

The `scope` column is drawn from `TBL-EXPOSURE-SCOPES`: `HISTORICAL_CUMULATIVE`
or `CURRENT_REVIEW_SCOPE`.

**Classification is by actual outputs revealed, not by lane or intent**
(Program v2 §0 rule 6).

**Program v2 §0B's auxiliary category** — `PURE_MECHANICAL_VERIFICATION`,
`DESIGN_INFORMING_MEASUREMENT`, `TARGET_PERFORMANCE_EXPOSURE` — is explanatory,
**not** a canonical enum, and may overlap. It is carried inside the `note`
column prefixed `class=`, never in the `classification` column, and it never
overrides that column.

**Program v2 §0B's other required fields** — sample snapshot id, session,
outputs actually revealed, seal timing, and downstream design use — are carried
inside the `note` column with the prefixes `sample=`, `session=`,
`revealed=`, `seal=` and `use=`. They live in the note rather than in extra
columns because the ratified six columns are fixed and a seventh would make this
ledger unparseable.

**Detection limit, acknowledged.** This ledger records only what was written
down. A clean exposure check is never evidence that no exposure occurred; it is
evidence that the records on file agree with each other. Failing to append an
exposure event is a per-se process violation.

**Timestamp convention.** `ts` values are UTC (`Z`). The V2 architecture
artifacts are dated **2026-09-08** in Aaron's local timezone, which is ahead of
UTC; the same events therefore carry `2026-09-07T…Z` here. This is a timezone
offset, not a discrepancy, and is recorded rather than silently normalised.
Where no contemporaneous record of a historical event's time exists, `ts` is
`UNKNOWN` and **no timestamp has been fabricated**.

---

## Pre-program state, and what this ledger does not claim

This ledger **opens at Wave-0 execution** and is prospective from that point. It
additionally carries **historical rows that are documented elsewhere in
committed bytes** — the six prior ETF-panel research paths, the 45-cell
robustness grid, and the design sessions that produced this program's
architecture. Those rows transcribe existing records; they do not reconstruct
sessions.

Everything **not** enumerated in the table is `UNKNOWN`, never `NONE`. In
particular:

- No no-peek scan, absence of code, or absence of a recorded outcome artifact is
  accepted as a substitute for a contemporaneous exposure record.
- The per-session timing, ordering and full content of the six historical ETF
  research paths were not recorded contemporaneously and are not reconstructed.
- Exposure to any market data **after** the frozen snapshot boundaries is
  `UNKNOWN_PENDING_AUTHORITATIVE_VERIFICATION` — see
  `research/extensions/LOCKBOX_PROCEDURE.md` §5. It is **not** `NONE`, and
  "this program did not download it" is explicitly insufficient.

---

## Downstream design-use link records (append-only)

Program v2 §0B requires downstream design use to be recorded as **append-only
link rows**, kept separately identifiable from the exposure events themselves.
They are lists, not a table, so that the event table stays the only table.

- **L1** — *from* event row 7 (the 45-cell robustness grid) *to* Map v1
  candidates **X11, X12, X13, X33**. Use: designs informed by the grid's
  **observed outcomes**; each carries lineage tag `ROBUSTNESS_GRID_INFORMED`.
  Recorded at `research/extensions/TSMOM_EXTENSION_RESEARCH_MAP_v2.md`
  Section C.
- **L2** — *from* event rows 1–7 (published ETF results) *to* every hypothesis
  `X01`–`X46` carrying `POST_EXPOSURE_DESIGN_ON_ETF_PANEL`. Use: hypothesis
  generation from exposed outcomes; **context T0** for any test on that panel.
  Recorded at the Map, Section C.
- **L3** — *from* the commodity-carry study's published record (**not** a
  TSMOM-EXT exposure event; see `research/extensions/TRIAL_LEDGER.md` §3.1)
  *to* candidates carrying `CARRY_STUDY_INFORMED`. Recorded at the Map,
  Section C.

**The Map is authoritative for per-candidate lineage tags; these link records
are the ledger-side index into it and must never diverge from it.** A new link
record is appended when a candidate's preregistration formally adopts an
exposed-outcome input.

---

## Cross-reference to the seat axis — carries no classification and no scope

These reviewer-seat events are recorded on the seat axis in
`ops/REVIEWER_EXPOSURE_LOG.md`. They are listed here **as pointers only**, as a
list rather than as rows, so that no reader mistakes them for research exposure
events and no aggregation can pick them up.

- **S1** — GPT-6 Astra, Round 1 design challenge: read the v1 architecture,
  which contains published target-metric figures.
- **S2** — GPT-6 Astra, Round 2 convergence: same, plus the Fable rebuttal.
- **S3** — GPT-6 Astra, fresh session: V2 document verification.
- **S4** — GPT-6 Astra: N1–N8 bounded corrections, delta verification, N7
  closure.
- **S5** — Claude Fable 5.1, architecture **author** (a producer seat, not a
  reviewer seat): its exposure is *also* researcher exposure and therefore
  **does** appear in the table below, as event rows 8 and 9.
- **S6** — Claude Opus 5, Wave-0 governance builder (a producer seat): appears
  below as event rows 10–12.

**Burning a reviewer seat consumes no research degrees of freedom.** S1–S4 move
no `N_trials` and are not events in the table below.

**Open, and left to Aaron (not a Wave-0 blocker):** whether a *pointer-only* row
carrying a non-conflicting classification should additionally sit in the event
table. That would touch an append-only authoritative ledger, so a builder does
not decide it unilaterally — the same disposition the ITSF precedent recorded
(`Intraday Trend Strategy Framework/ops/REVIEWER_EXPOSURE_LOG.md`).

---

## Normalized values as of the last row below

- `HISTORICAL_CUMULATIVE` — rows 1–9, all `REVEALED_TARGET_METRIC` — normalizes
  to **`TARGET_METRIC`**.
- `CURRENT_REVIEW_SCOPE` — rows 10–43; rows 10, 14 and 27 are
  `REVEALED_TARGET_METRIC` and rows 11–13, 15–26, 28–**43** are `NO_OUTCOME` → `NONE` —
  normalizes to **`TARGET_METRIC`**. *(This enumeration was stale at rows 25–26
  and is corrected here; it is a derived reading, never a stored authority.)*

This is a derived reading of the rows, restated for convenience. It is **not** a
stored authority — the rows are. `qros-state.yaml` declares
`outcome_exposure: {declared: "TARGET_METRIC", scope: "HISTORICAL_CUMULATIVE"}`,
which agrees.

---

## Wave-0 sibling artifacts this ledger is read with

- `ops/REVIEWER_EXPOSURE_LOG.md` — seat axis
- `research/extensions/SAMPLE_REUSE.md` — sample-reuse contract
- `research/extensions/TRIAL_LEDGER.md` — the four-object trial record
- `research/extensions/LOCKBOX_PROCEDURE.md` — snapshot, protection and release
- `research/extensions/DATA_INVENTORY_SPEC.md` — inventory specification (not run)
- `research/extensions/WAVE0_VERIFICATION_RECORD.md` — canonical verification outcomes
- `qros-state.yaml` — declared state; names both ledgers in `inputs[]`

---

## Append log

- **2026-09-14 (UTC)** — **TSMOM-VRP-01 S3 governed Stage-A run.** Event rows
  **50–51**. Row 50 is `GENERATED_NOT_SEEN`: the ONE governed Stage-A run under
  `VRP-AUTH-0001` constructed the real monthly excess-return series and its sealed
  bootstrap interval straight into the VRP protected store, printing integrity
  metadata only. **That row is the event that spends the governed trial** — VIX-chain
  `N_trials` 0 → 1. Row 51 is the **single authorised reveal** under the separate
  grant `VRP-AUTH-0002`, committed only after the post-compute gate passed 19/19:
  `REVEALED_TARGET_METRIC`, `reveal_count = 1`, state **UNRESOLVED / Class 3**.
  Stage B was not computed and, under the sealed §O stop rule, never runs on this
  historical result; R6–R11 were neither computed nor revealed. C-A exposure NONE.
  **No existing row was edited or reordered.**
- **2026-09-14 (UTC)** — **TSMOM-VRP-01 S2 acceptance.** Event rows **48–49**
  appended, both `NO_OUTCOME` / `PURE_MECHANICAL_VERIFICATION`. Row 48 records the
  **S1 seal** retrospectively — drafted by S1 in
  `research/extensions/vrp/VRP_EXPOSURE_DISCLOSURE.md` §5 and appended by the accepted
  S2 session, exactly as that document directs — with the design-exposed episode list
  attached by reference. Row 49 records the **S2 acquisition and mechanical validation**
  of the new `dataset.cboe.vix-futures-monthly-chain` sample: raw Cboe settlements read
  for construction mechanics only, with **no return, carry, basis or strategy statistic
  computed on the real chain** and Stage A / Stage B exercised on synthetic fixtures only.
  **No trial was spent and no scientific outcome was generated or revealed**; the
  governed-run gate refuses real data and no Owner execution authorisation exists.
  C-A exposure **NONE**. **No existing row was edited or reordered.**
- **2026-09-07 (UTC)** — Ledger created at Wave-0 execution by the Wave-0
  governance session (Claude Opus 5), under Aaron's
  `AUTHORIZE_WAVE_0_GOVERNANCE_EXECUTION`. Event rows 1–12; link records L1–L3;
  seat cross-reference S1–S6 (pointer-only).
- **2026-09-07 (UTC)** — Wave-1 execution, under Aaron's
  `AUTHORIZE_WAVE_1_RESEARCH_EXECUTION_WITH_GATES`. Event rows **13–16**
  appended: preflight and governance transition (13, no outcome); the
  **X07/X45/X46 edge diagnostics** (14, target-metric on the burned ETF panel);
  the Databento input verification (15, metadata only); and the settlement /
  open-interest extraction (16, raw inputs only). **No existing row was edited
  or reordered.** No candidate research ran, no protected outcome was opened,
  no data was purchased, and `X01_FULL_PERFORMANCE_EXECUTED = NO`.
- **2026-09-08 (UTC)** — Event row **17** appended: the X02a / X03 attempt, which
  was **BLOCKED** by a panel-construction defect in this session's own code. No
  outcome was revealed and no design decision rests on it. **No existing row was
  edited or reordered.**
- **2026-09-08 (UTC)** — **X02 production cash-path repair.** Event row **24**
  appended, with `ts` bounded by observed artifact mtimes. The TSMOM production
  return path now implements the frozen Sec 5 cash convention, so the previously
  disclosed roll-day divergence is repaired rather than carried forward. **No
  existing row was edited or reordered**; no strategy-return series was
  constructed, so `N_trials` does not move.
- **2026-09-08 (UTC)** — **Astra cash-ledger repair.** Event rows **22–23**
  appended: row 22 is a **second timestamp ERRATUM**, correcting row 21's
  claimed observed interval (which contained 71 minutes of future time at the
  moment it was written); row 23 records this repair, with `ts` bounded by
  observed artifact mtimes. **No existing row was edited or reordered.** No
  strategy-return series was constructed, so `N_trials` does not move.
- **2026-09-08 (UTC)** — Bounded **Fable-audit repair**. Event rows **20–21**
  appended: row 20 is a **timestamp ERRATUM** correcting the projected `ts`
  values on rows 18–19 (raised by the audit, confirmed against the session
  record); row 21 records the repair's own computation, with a bounded observed
  interval rather than a projected instant. **No existing row was edited or
  reordered**, and no classification, scope or outputs field of rows 18–19 was
  changed — only their timing is corrected.
- **2026-09-08 (UTC)** — Bounded contract-identity repair, under Aaron's
  authorization. Event rows **18–19** appended: the corrected re-extraction on
  persistent contract keys (18), and the panel-sanity gate plus the gated
  X02a/X03 reruns (19). Both `NO_OUTCOME` — the repair is mechanical and
  constructs no strategy-return series, so **`N_trials` does not move** and the
  Databento frozen reference stays **14**. Row 17's invalid attempt is
  **retained, not deleted**: implementation-error history, not a strategy trial.
  **No existing row was edited or reordered.**

---

## Ledger

| ts | scope | classification | granularity | artifact_or_pointer | note |
|---|---|---|---|---|---|
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `quant-research-knowledge-base/relationships/relationships.csv` line 16 → `strat.tsmom.multi-asset-core`; repo `DESIGN_DECISIONS.md`, `STUDY_SUMMARY.md`, `output/` | Row 1. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN (no contemporaneous record; not reconstructed). revealed=full-sample core results — Sharpe, sleeve and per-asset attribution, drawdown, cost sweep, net monthly stream; the figures live in the committed reports and are not restated here. use=baseline definition, frozen at HEAD `c63114a`; design parent of every candidate tagged `POST_EXPOSURE_DESIGN_ON_ETF_PANEL` (link L2). seal=none — the core had no preregistration (verified by absence). |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `relationships.csv` line 17 → `strat.tsmom.crash-defense-overlay`; `output/PHASE0_SYSTEMIC_VERIFICATION.md` | Row 2. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=premise-test outputs on the panel; overlay `not_promoted`. use=cluster C11/C12 lineage; Map v2 §B.1 forbids repackaging the mechanism. seal=none. |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `relationships.csv` line 18 → `strat.tsmom.vol-breakout-overlay`; `output/BREAKOUT_PHASE1B_PREMISE.md` | Row 3. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=premise-test outputs; overlay `not_promoted`. use=cluster C4/C10 lineage. seal=none. |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `relationships.csv` line 19 → `strat.tsmom.seasonality-overlay`; `output/SEASONALITY_PHASE1_PREMISE.md` | Row 4. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=premise-test outputs; overlay `not_promoted`. use=Map v2 §B.1 exclusion. seal=none. |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `relationships.csv` line 20 → `strat.tsmom.yield-spread-overlay`; `research/yield_spread/PHASE1_PREMISE.md` | Row 5. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=premise-test outputs; overlay `not_promoted`. use=cluster C12 lineage. seal=none. |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `relationships.csv` line 21 → `strat.xsmom.multi-asset`; `output/XSMOM_REPORT.md` | Row 6. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=cross-sectional momentum results; `falsified` at 0/5 in a pre-registered multi-universe replication. use=Map v2 §B.1 exclusion; XSMOM is quarantined under `_quarantine_xsmom/`. seal=pre-registered multi-universe replication, per the KB README decisiveness note. |
| UNKNOWN | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | `robustness.py`; the 45-combination robustness grid **inside** the core's record | Row 7. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`. session=UNKNOWN. revealed=grid outcomes across 45 parameter combinations. **This sits inside the core's record (row 1) as robustness, not selection; it is NOT a seventh independent burn and does not change the verified 6-of-6 count.** use=lineage tag `ROBUSTNESS_GRID_INFORMED` (link L1). seal=none. |
| 2026-09-07T17:36Z | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | V1 architecture `research/extensions/TSMOM_EXTENSION_RESEARCH_MAP.md` sha256 `76b902ed6e244fd2bb8293c1a55191c8b468759fcd5b1eeda7d10465be1d15f5`; `TSMOM_EXTENSION_RESEARCH_PROGRAM.md` sha256 `7495faa976357592f50243e4f6b0342d557cc47193be85df938ee8e26b79040b` | Row 8. class=TARGET_PERFORMANCE_EXPOSURE + DESIGN_INFORMING_MEASUREMENT. sample=`dataset.yfinance.multi-asset-etf-panel`, read of published results only. session=Claude Fable 5.1, architecture author, invoked directly by Aaron (seat S5). ts is approximate, from file mtimes `2026-09-08 01:35`/`01:36` local. revealed=the published full-sample core, sleeve, 45-cell robustness-grid, cost-sweep and overlay results; self-declared in the routing header as `OUTCOME_EXPOSED=TARGET_METRIC`; **no computation was run to produce the architecture**. use=design lineage of `X01`–`X46`; per-candidate tags are carried in `TSMOM_EXTENSION_RESEARCH_MAP_v2.md` Section C, which is authoritative for them (link L2). seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-07T19:37Z–19:58Z | HISTORICAL_CUMULATIVE | REVEALED_TARGET_METRIC | TARGET_METRIC | V2 architecture `…MAP_v2.md` sha256 `e9555a0220b58fdf6dfaee40613c5b28b114ea02b2a94137b1a23c56a87dbace`; `…PROGRAM_v2.md` sha256 `e9555f561e302c4f937a05829bd23b2368c79765b4c275b788f8fe14264d46c2`; `idea_registry/IDEA_REGISTRY_v2.csv` sha256 `7e3024edd1e553745c75f83f3f107df84d2814ae06be4876f53aae117108717c`; `DASHBOARD_v2.md` sha256 `951801bcea0281811b501ca19f63b26a3f3e488ef7cd4ca28a1f6641bf5e19b7` | Row 9. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`, re-read of already-published results. session=same author seat, continuation window (S5). ts from file mtimes `2026-09-08 03:37`–`03:58` local. revealed=routing header verbatim, "published results read in v1 design; nothing new computed in this pass". **Re-exposure / provenance event: no new outcome was generated.** use=the converged V2 architecture, accepted by Aaron. seal=`PREREG_SEALED=N/A`. |
| 2026-09-07T20:00Z–20:40Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | This Wave-0 governance session; artifacts = this file and its Wave-0 siblings | Row 10. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.yfinance.multi-asset-etf-panel`, one already-published headline figure. session=Claude Opus 5, Wave-0 governance builder (seat S6). revealed=**disclosed rather than minimised** — while reading the Map's exposure statement this session read the baseline's published headline Sharpe, a target-metric figure on the ETF panel. No results file was opened: `output/monthly_returns.csv`, `output/dd_per_asset_net.csv` and `output/monthly_portfolio_weights.csv` were **not** read. No backtest, Sharpe, return series, drawdown or performance comparison was computed. use=Wave-0 governance artifacts only; **no candidate design decision was taken**. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-07T20:05Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `c1-drag-audit/SAMPLE_REUSE.md`; `mean-reversion-research/protocol/SAMPLE_REUSE.md`; `commodity-carry-research/preregistration/PREREGISTRATION.md` §10 | Row 11. class=DESIGN_INFORMING_MEASUREMENT. sample=**other projects' samples, not a TSMOM-EXT sample**. session=Claude Opus 5, Wave-0 governance builder. revealed=**disclosed cross-project read** — this session read the commodity-carry study's registry-public Deflated Sharpe Ratios, quoted verbatim inside `c1-drag-audit/SAMPLE_REUSE.md`, and the carry arms' `NOT_PROMOTED` verdicts. Those are **another program's outcomes**, not TSMOM-EXT outcomes, so **TSMOM-EXT research exposure is unaffected** — the same treatment `mean-reversion-research/ops/EXPOSURE_LEDGER.md` applies to the identical situation. Recorded so the read is visible rather than implicit. use=establishing the verified `N_trials = 14` convention in `research/extensions/TRIAL_LEDGER.md`. seal=n/a. |
| 2026-09-07T20:12Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `data/close_prices_raw.csv`; `data/fetch_metadata.csv`; `commodity-carry-research/data/MANIFEST.md`; `src/signals.py::to_monthly`; `config.py:154` | Row 12. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.yfinance.multi-asset-etf-panel` and `dataset.databento.commodity-futures-curves`, metadata only. session=Claude Opus 5, Wave-0 governance builder. revealed=snapshot-boundary verification only — file SHA-256, row count, first and last **date**, per-ticker row counts, vendor request parameters, and the monthly-resample convention. **No price level, no return, no strategy output.** use=lockbox snapshot identity, `LOCKBOX_PROCEDURE.md` §2; results recorded in `research/extensions/WAVE0_VERIFICATION_RECORD.md`. seal=n/a. |
| 2026-09-07T21:40Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/WAVE1_PREFLIGHT.md`; `qros-state.yaml` | Row 13. class=PURE_MECHANICAL_VERIFICATION. sample=n/a. session=Wave-1 execution session (Claude Opus 5), under Aaron's `AUTHORIZE_WAVE_1_RESEARCH_EXECUTION_WITH_GATES`. revealed=nothing — runtime preflight only: `qros check` / `status` / `next`, the Wave-0 validator (124 checks, 0 failed), the four V2 hashes, and the lane/stage transition test. use=the governance state transition recorded in `WAVE1_PREFLIGHT.md`. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-07T22:05Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | `research/extensions/diagnostics/EDGE_DIAGNOSTICS.md`; `research/extensions/diagnostics/edge_diagnostics.json` | Row 14. class=TARGET_PERFORMANCE_EXPOSURE + DESIGN_INFORMING_MEASUREMENT. sample=`dataset.yfinance.multi-asset-etf-panel`, context T0. session=Wave-1 execution session (Claude Opus 5). revealed=**X07/X45/X46 diagnostics** — new statistics of the already-burned published streams: 17x17 trend-PnL correlation matrix, ENB (11.17 participation / 13.53 entropy), sleeve correlations; average net exposure by sleeve, a HAC factor regression on published net returns, and a static-book/residual Sharpe decomposition (0.783 / 0.776 / 0.153 gross); a quadratic fit vs SPY, long/short and crisis attribution, and tail dependence. Reconciled to the published gross/net streams at max abs diff 2.0e-05. **No candidate was evaluated, no parameter tuned, no baseline change proposed** (MAP_v2 anti-snooping rule). use=calibrates MAP_v2 Section E priors for candidates modifying this stream, and informs the X01 draft's crisis-diagnostic scoping. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-07T22:20Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/DATABENTO_W1_INPUT_VERIFICATION.md`; `research/extensions/wave1/databento_probe.json` | Row 15. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`, metadata and structure only. session=Wave-1 execution session (Claude Opus 5). revealed=dataset identity and file hash, DBN metadata, per-root record/contract counts, first/last session dates, settlement price RANGES (units), spread-vs-outright composition, and open-interest availability. **No return, no PnL, no Sharpe, no strategy output; no protected outcome opened; no data purchased.** Findings: last session 2026-06-30 (the June-30/July-1 ambiguity resolved); 79.1% of the delivery is calendar spreads; the eight cents-quoted roots requiring divisor 100 identified from observed price levels. use=establishes the two binding X02a construction requirements (outright-only filtering; price-unit divisor). seal=n/a. |
| 2026-09-07T22:35Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/extract_settle_oi.py`; `settle_panel.parquet`; `oi_panel.parquet` | Row 16. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`. session=Wave-1 execution session (Claude Opus 5). revealed=daily SETTLEMENT prices and OPEN INTEREST per outright contract for the 18 roots, extracted from the `statistics` schema as an INPUT to X02a/X03. These are raw vendor market-data inputs, not outcomes: **no position, no return series, no PnL and no strategy statistic was formed from them in this event.** use=input panels for the X02a dollar-ledger identity work and the X03 structural roll diagnostics. seal=n/a. |
| 2026-09-08T00:20Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/FUTURES_INFRA_TRUTH.md`; `x02a_results.json`; `x03_structural.json` | Row 17. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`. session=Wave-1 execution session (Claude Opus 5). revealed=**X02a and X03 attempted and BLOCKED.** Computed: A1 front-contract series, chained held-contract return index, a one-contract dollar ledger and its reconciliation residual, truncation invariance, roll monotonicity, and structural roll-rule comparison metrics. **No strategy-performance metric: no Sharpe, no portfolio return, no wrapper comparison, no candidate selection.** The single-contract return index is an accounting object used to test an identity, not a strategy. All per-root results are **INVALID** and are not relied on: the panels were keyed by raw symbol, which repeats on CME's 10-year cycle and merged two contracts per column. use=none — the results inform no design decision; the defect and its fix are recorded in `FUTURES_INFRA_TRUTH.md`. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-08T01:10Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/extract_contracts_v2.py`; `settle_v2.parquet`; `oi_v2.parquet`; `contracts_meta.parquet` | Row 18. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`, existing local files only. session=Wave-1 contract-identity repair session (Claude Opus 5), under Aaron's bounded X02/X03 repair authorization. revealed=re-extraction of daily SETTLEMENT (`stat_type` 3) and OPEN INTEREST (`stat_type` 9) per **persistent contract key** `instrument_id__expiration_date`, joined to the `definition` schema on (date, instrument_id) and filtered to `instrument_class == 'F'`. Raw vendor inputs only: **no position, no return series, no PnL, no Sharpe and no strategy statistic was formed.** No purchase, no refresh, no protected data. use=replaces the INVALID raw-symbol panels as the input to X02a/X03. seal=n/a. |
| 2026-09-08T01:55Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/panel_sanity.py`; `run_x02a_v2.py`; `run_x03_structural_v2.py`; `FUTURES_INFRA_TRUTH.md` | Row 19. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`. session=Wave-1 contract-identity repair session (Claude Opus 5). revealed=panel-sanity gate results (contract-key uniqueness, decade-collision elimination, expiry coherence, observed-life sanity, outright-only, root coverage); then, gated on that PASS, the X02a v2 mechanical results (A1 front series, roll counts and monotonicity, held-contract availability, chained held-contract return index, one-contract dollar-ledger reconciliation residual, truncation invariance, cost/unit regression) and the X03 v2 structural roll-rule comparison. **No strategy-performance metric: no Sharpe, no portfolio return, no wrapper comparison, no candidate selection, no roll-rule selection on performance.** The one-contract return index is an accounting object used to test an identity. use=X01 preconditions 1-2; roll-rule DoF determination. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-08T00:37:44Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | **ERRATUM to rows 18 and 19** (this file) | Row 20. class=PURE_MECHANICAL_VERIFICATION. **Timestamp erratum, raised by the Fable audit §10 item 1 and confirmed.** Rows 18 and 19 carry `ts` values of 01:10Z and 01:55Z, but both rows were APPENDED at **2026-09-08T00:37:44Z** - before the re-extraction finished (01:08Z) and before the panel-sanity gate and reruns (01:10-01:11Z). 01:55Z corresponds to no event at all; the last artifact write was 01:13Z. Those `ts` values were **projected, not observed**. **Corrected reading:** row 18's extraction completed at **01:08:32Z**; row 19's gate ran **01:10:18Z** and its X02a/X03 reruns completed by **01:13Z**; both rows were written at **00:37:44Z**, ahead of the work. Append-before-run is acceptable; a projected time presented as a realized one is not. **No classification, scope, sample or outputs-revealed field of rows 18-19 is corrected - only their timing**, and the rows themselves are not edited. No timestamp has been fabricated: 00:37:44Z is the observed append time and the others are observed completion times. seal=n/a. |
| 2026-09-08T02:05Z-03:35Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/run_x02a_v3.py`; `run_x03_structural_v3.py`; `x02a_v3_results.json`; `x03_structural_v3.json`; `FABLE_REPAIR_RECORD.md` | Row 21. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves` and, for the sign-agreement diagnostic only, the frozen ETF cache. session=Wave-1 Fable-audit repair session (Claude Opus 5). **ts is a bounded interval of observed activity, not a projection.** revealed=costed dollar ledger with roll-cost legs; gross and net ledger reconciliation residuals; end-to-end cost-path regression and its sabotage failure; **sign-agreement rates between the ETF and futures composite signals per mapped pair** (USO/CL 91.7%, UNG/NG 90.6%, GLD/GC 94.5% over 181 months); held-front jump counts; structural roll-rule disagreement on the authoritative carry comparator; the OI publication lag. **No Sharpe, no portfolio return, no cumulative PnL, no wrapper performance comparison, no candidate selection, no roll-rule selection.** The sign-agreement diagnostic compares SIGNAL SIGNS, not returns. use=X01 preconditions 1-2 (builder-side, pending independent verification); roll-rule DoF declaration. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-08T02:21:46Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | **ERRATUM to row 21** (this file) | Row 22. class=PURE_MECHANICAL_VERIFICATION. **Second timestamp erratum, raised by the fresh GPT-6 Astra delta verification and confirmed from observed evidence.** Row 21 states `2026-09-08T02:05Z-03:35Z` and calls it a bounded OBSERVED interval. It was not: this file's own write time is **02:21:46Z**, so **03:35Z lay 71 minutes in the FUTURE at the moment the row was appended**, and 02:05Z precedes any artifact of that phase. The row therefore repeated the very projected-time defect that erratum row 20 had just corrected. **Corrected reading, from observed file mtimes only:** the Fable-repair phase's artifacts span **02:15:45Z** (`run_x03_structural_v3.py`, earliest) to **02:24:51Z** (`FABLE_REPAIR_RECORD.md` and `qros-state.yaml`, latest); this ledger and the reviewer log were both written at **02:21:46Z**. The phase's START is **UNKNOWN** and is bounded above by 02:15:45Z; no start time is invented. **Nothing but row 21's timing is corrected** - its classification, scope, sample, outputs-revealed and use fields all stand, and row 21 is not edited. seal=n/a. |
| 2026-09-08T02:44Z-02:51Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/run_x02a_v3.py`; `x02a_v3_results.json`; scratch mutation tests | Row 23. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`. session=Wave-1 Astra cash-ledger repair session (Claude Opus 5). **ts is bounded by OBSERVED artifact mtimes** (`run_x02a_v3.py` 02:50:20Z, `x02a_v3_results.json` 02:50:26Z) and this row is appended after them, not before. revealed=one-contract PRIMITIVE cash ledger (exit/entry legs in dollars, quantity, roll event); the synthetic CL $12.50+$12.50=$25.00 regression; the quantified divergence between the percentage convention and the primitive cash convention on roll days; three scratch mutation results. **No Sharpe, no portfolio return, no cumulative PnL, no wrapper comparison, no candidate or roll-rule selection.** X03 was NOT rerun. use=X02 accounting truth; X01 precondition 1 remains PENDING. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-08T03:05Z-03:25Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/wave1/run_x02a_v3.py`; `x02a_v3_results.json`; scratch mutation tests A-D | Row 24. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.databento.commodity-futures-curves`. session=Wave-1 X02 production cash-path repair session (Claude Opus 5). **ts is bounded by OBSERVED artifact mtimes**, and this row is appended after the work, not before. revealed=the TSMOM-local production chain `tsmom_chain_net_returns` and its cash legs; the production synthetic CL roll cost ($25.00); roll-day and non-roll reconciliation residuals; event-semantics results; the production divisor regression; four mutation outcomes; the measured legacy carry gap. **No Sharpe, no portfolio return, no cumulative PnL, no wrapper comparison, no candidate or roll-rule selection.** X03 was NOT rerun; the panel gate and OI field were not altered. use=X02 accounting truth; X01 precondition 1 remains PENDING. seal=none; `PREREG_SEALED=N/A`. |
| 2026-09-08T07:14:31Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | **ERRATUM to row 24** (this file) | Row 25. class=PURE_MECHANICAL_VERIFICATION. **Third timestamp erratum, raised by the fresh GPT-6 Astra delta verification and confirmed from observed evidence.** Row 24 states `2026-09-08T03:05Z-03:25Z` and calls it "bounded by OBSERVED artifact mtimes". It is not. **This file's own write time for row 24 is 03:05:06.515Z** (observed mtime; Astra independently placed successful creation at approximately 03:05:06.990Z), so **03:25Z lay about 20 minutes in the FUTURE** at the moment the row was appended. The stated START is wrong in the other direction: the phase's earliest artifact, `run_x02a_v3.py`, has mtime **03:02:16.046Z**, which PRECEDES the claimed 03:05Z start - so the interval `03:05Z-03:25Z` does not even contain the artifacts it purports to bound. **This is the same defect class as errata rows 20 and 22, now occurring a third time.** **Corrected reading, from observed file mtimes only:** the production cash-path phase's artifacts span **03:02:16.046Z** (`run_x02a_v3.py`) to **03:05:18.759Z** (`qros-state.yaml`), with `x02a_v3_results.json` at 03:02:24.237Z and `FUTURES_INFRA_TRUTH.md` plus both ledgers at 03:05:06.514-03:05:06.515Z. The phase's START is **UNKNOWN** and is bounded above by 03:02:16.046Z; no start time is invented. **Nothing but row 24's timing is corrected** - its classification, scope, sample, outputs-revealed and use fields all stand, and row 24 is not edited. **Standing convention adopted to stop the recurrence:** a `ts` may hold only (a) a clock value observed BEFORE the append, or (b) an interval whose endpoints are already-observed artifact mtimes - never a projected endpoint. **This row's own `ts` is an observed clock read taken immediately before this append and is therefore a LOWER BOUND on it, not a projection.** seal=n/a. |
| 2026-09-08T07:19:45Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/X01_PREREGISTRATION_DRAFT.md` | Row 26. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset was read in this phase**. session=Wave-1 two-item documentation/governance closure session (Claude Opus 5). **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it, per the standing convention now recorded in `REVIEWER_EXPOSURE_LOG.md` section 2A.** revealed=**nothing.** This phase edited documentation only: the X01 draft's active cost route was pinned to the independently accepted cash-first implementation `run_x02a_v3.py::tsmom_chain_net_returns` (new binding section 3.4; section 3 cost row and section 3.2 divisor requirement restated against it), and the legacy carry percentage callables were demoted to historical/reconciliation reference. **No computation was run: no panel was loaded, no return, cost, residual, Sharpe, PnL, wrapper comparison, candidate or roll-rule selection was produced, and no prior result was re-read for its values.** X02 and X03 implementation and results bytes are unchanged and were verified byte-identical. use=X01 construction contract; **X01 precondition 1 remains PENDING_INDEPENDENT_VERIFICATION and is NOT marked MET**. seal=none; `PREREG_SEALED=NO`. |
| 2026-09-08T08:08:17Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | `research/extensions/x01/X01_PREREGISTRATION_DRAFT.md`; `research/extensions/review_history/X01_STAGE_A2_DESIGN_REVIEW_2026-09-08.md` | Row 27. class=DESIGN_INFORMING_MEASUREMENT. sample=the **frozen ETF panel's published baseline output** (read) and the accepted Databento panels (structural metadata only). session=Wave-1 X01 Stage-A2 bounded preregistration repair session (Claude Opus 5). **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it** (standing convention, `REVIEWER_EXPOSURE_LOG.md` section 2A). revealed=**this row is classified `REVEALED_TARGET_METRIC` deliberately and is NOT minimised.** While establishing that no standalone commodity-sleeve monthly stream is published, this session read `output/dd_sleeve_summary.csv`, which carries the published baseline's **portfolio-scoped Commodity-sleeve summary statistics (Sharpe, annualised return, annualised vol, max drawdown, hit rate, PnL contribution)**. That is already-published T0 material and this seat's cumulative classification was already `REVEALED_TARGET_METRIC`, but it is **adjacent to the `E` leg of the X01 estimand** and is recorded explicitly rather than left implicit. Also read: contract counts, first-settlement dates and coverage fractions from the accepted futures panels (structural, no returns); and a closed-form precision formula evaluated on **declared constants only**. **NO X01 target quantity was constructed or observed: no `dS`, no leg Sharpe on the X01 window, no paired series, no PnL, no bootstrap, no crisis statistic.** X02 and X03 were NOT rerun; no protected-forward data was opened; no data was purchased. use=X01 preregistration repair. **No design value was chosen from any outcome** - the block length was fixed on a signal-mechanism argument, the crisis window adopted from a pre-existing canonical constant, the basket options taken from pre-existing pre-outcome c1 D5 records, and `B` was NOT chosen at all. seal=none; `PREREG_SEALED=NO`; trial contribution **0** (a design review constructs no strategy-return series). |
| 2026-09-08T08:29:05Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/X01_PREREGISTRATION_DRAFT.md` | Row 28. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset, no panel and no results file was read in this phase.** session=Wave-1 X01 O-3 statistical-wording closure session (Claude Opus 5). **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it** (standing convention, `REVIEWER_EXPOSURE_LOG.md` section 2A). revealed=**nothing.** Bounded wording repair to the preregistration only: the block-length justification restated as a prospectively chosen design-based dependence horizon sealed as an inference convention rather than a theorem about the true dependence structure; the monotonic-CI-widening claim deleted and no longer used as the anti-steering argument; the Jobson-Korkie/Memmel prospective-precision calculation's role pinned by five explicit disclaimers. **Every O-3 mechanic is unchanged** (stationary bootstrap, L = 12 months, paired monthly resampling, 10,000 replicates, 95 percent percentile CI, master seed 7, degenerate handling, multiplicity). **No computation of any kind was run: no return, cost, residual, Sharpe, correlation, bootstrap or crisis statistic**, and the precision table's numbers were not recomputed. No target-data-dependent block selection was added and no bootstrap sensitivity family was created. X02/X03 not reopened; V2 not altered; no protected-forward access; no data purchased. use=X01 inference contract. **O-1, O-2 and O-6 options, candidate values, meanings and recommendations are unchanged**; the only edit inside O-2 replaced the same over-strong 'narrowest-possible bound' qualifier, carrying no decision content. seal=none; `PREREG_SEALED=NO`; trial contribution **0**. |
| 2026-09-08T08:43:49Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/X01_PREREGISTRATION_DRAFT.md`; `research/extensions/review_history/X01_STAGE_A2_DESIGN_REVIEW_2026-09-08.md` | Row 29. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset, panel or results file was read in this phase.** session=Wave-1 X01 owner-decision incorporation session (Claude Opus 5), under Aaron's `O1_OWNER_DECISION` / `O2_OWNER_DECISION` / `O6_OWNER_DECISION`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it** (standing convention, `REVIEWER_EXPOSURE_LOG.md` section 2A). revealed=**nothing.** Text/design incorporation only: Aaron's three owner decisions written into the active contract - ag proxy `ZC ZS ZW LE HE GF` equal 1/6 (`FIXED_PROXY_WITH_COMPOSITION_MISMATCH`), `B = 0.15` annualised-Sharpe units with boundary -0.15, and `SYMMETRIC_ZERO_CARRY` - with their OPEN/UNSET status removed, the rejected alternatives relabelled REJECTED and non-executable, and the escape-route register closed to `NONE`. **NO computation was run: no X01 paired return stream constructed, no Sharpe(F), no Sharpe(E), no dS, no bootstrap, no crisis statistic, no A1/S1/S2 performance, and no X01 target result exposed.** `TARGET_X01_OUTCOME_ACCESSED = NO`. The decisions were taken and recorded **prospectively, before any such quantity existed.** X02/X03 not reopened and their bytes unchanged; V2 unchanged; no protected-forward access; no data purchased; no strategy construction. **Option A of O-1 and Option 2 of O-6 are NOT evaluated and produce no series, no attempt row and no trial.** use=X01 contract completion. seal=none; `PREREG_SEALED=NO` - **an owner design decision is not authorization to execute**; trial contribution **0**. |
| 2026-09-08T09:04:12Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/X01_PREREGISTRATION_DRAFT.md`; `research/extensions/review_history/X01_STAGE_A2_DESIGN_REVIEW_2026-09-08.md` | Row 30. class=PURE_MECHANICAL_VERIFICATION. sample=**none of the X01 target sample.** The only computation run was on a **hand-built synthetic month-end price path** and pure arithmetic. session=Wave-1 X01 four-defect A2 repair session (Claude Opus 5). **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** Four bounded design repairs after `X01_A2_DELTA_VERDICT = FAIL`: (1) the active signal restored to the frozen V2 **mean of signs** with the zero convention read from `src/signals.py` rather than invented; (2) the portfolio rebalance cost moved from the invalid change-in-position-magnitude expression to **absolute traded contract quantity per contract identity**, leaving the accepted X02 per-side dollar primitive untouched; (3) the S1 forward ratio adjustment given one exact equation with the corrected direction and frozen roll timing; (4) secondary inference **decided** as descriptive-only with `BH_FDR_REQUIRED=NO` and no materiality gate. February-2020 month-end corrected to 2020-02-29. **Verification used synthetic inputs only**: a constructed monthly path whose horizon returns are (+0.10,+0.10,+0.10,-0.01), five position-change cases, and a 100/110/121 roll arithmetic. **NO X01 paired series, Sharpe(F), Sharpe(E), dS, bootstrap or crisis statistic was constructed or opened.** `TARGET_X01_OUTCOME_ACCESSED = NO`. The frozen baseline `signal_method_b` was executed on the synthetic path only - **no ETF or futures panel was loaded**. X02/X03 not reopened and their bytes unchanged; V2 unchanged; no protected-forward access; no data purchased; no strategy construction. **O-1, O-2 and O-6 unchanged.** use=X01 contract completion. seal=none; `PREREG_SEALED=NO`; trial contribution **0**. |
| 2026-09-08T09:20:52Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `qros-state.yaml` (prereg pointer block only) | Row 31. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset, panel or results file was read.** session=Wave-1 X01 seal-attempt session (Claude Opus 5), under Aaron's `AARON_OWNER_DECISION = SEAL_X01_PREREGISTRATION`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**nothing.** **THE SEAL DID NOT COMPLETE.** Byte-drift check ran first and **PASSED**: the preregistration hashes to `9c7b104f...96986743` and the A2 record to `173d138e...030acebe`, both **exactly matching** the bytes Aaron pinned, so there is **no BYTE_DRIFT** and the reviewed scientific contract is intact and untouched by this session. The seal stopped on the **canonical QROS mechanism's own input**: `qros_runtime/eligibility.py::seal_state` derives the seal by resolving `prereg.seal_revision` as a git revision and comparing the preregistration blob at that revision against HEAD, and **the preregistration exists in no commit, branch or tag** (`git log --all -- <path>` empty; `cat-file -e HEAD:<path>` fatal; 0 tags), because the whole `research/extensions/` tree is untracked - while this turn's hard stops forbid a commit. A non-resolving value would derive `SEAL_UNVERIFIABLE`, not `VERIFIED`, so writing one would have forced the seal rather than made it. **The preregistration bytes were NOT modified and no seal marker was written.** The only change is the canonical `prereg` pointer block added to `qros-state.yaml` in its documented **null pre-seal** form (spec section 2.4), which is correct for the current state and pre-positions the pointer. **No X01 target series, Sharpe, dS, bootstrap or crisis statistic was constructed or opened; no performance ran; nothing was sealed.** `TARGET_X01_OUTCOME_ACCESSED = NO`. use=seal governance. seal=**NOT SEALED**; `PREREG_SEALED=NO`; trial contribution **0**. |
| 2026-09-08T09:32:49Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | seal-base revision `df5b28ab7324c7ba789ab231431f077288c3fd84`; `qros-state.yaml` | Row 32. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset, panel or results file was read.** session=Wave-1 X01 seal-execution session (Claude Opus 5), under Aaron's `AARON_OWNER_DECISION = AUTHORIZE_ONE_LOCAL_X01_SEAL_BASE_COMMIT`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**nothing.** **THE X01 PREREGISTRATION IS NOW SEALED.** Ordering, which is the point: the fresh GPT-6 Astra Stage A2 delta review returned PASS with 0 design blockers and 0 owner decisions remaining **before** the seal; Aaron adopted O-1, O-2 and O-6 **before** any target-outcome access; Aaron's seal authorization **preceded** the seal; and **no X01 target outcome had been computed or opened at any point before or during the seal.** Byte-drift check ran first and PASSED - the preregistration and the A2 record hashed exactly to the reviewed pins. Exactly **one local commit** was created as the seal carrier (`df5b28ab7324c7ba789ab231431f077288c3fd84`, message `research: X01 preregistration seal base`), staging only the authorized roots `research/extensions/`, `ops/` and `qros-state.yaml`; 47 text files; parquet panels and caches were excluded by the repository's own pre-existing `.gitignore`; **nothing was pushed** and the branch is local-only, one commit ahead of `origin/main`. The committed preregistration blob hashes to `9c7b104f...96986743` at both the seal revision and HEAD. `prereg.seal_revision` was then set through the canonical `qros-state.yaml` mechanism and the runtime **derives `SEAL=VERIFIED`**; **no `seal_valid`, `seal_validity` or `seal_state` field is stored anywhere in the structure** - seal validity remains derived, and no second sealing mechanism was invented. **The scientific contract was NOT edited to make the seal**: the preregistration is byte-identical before and after. **No X01 target series, Sharpe(F), Sharpe(E), dS, bootstrap or crisis statistic was constructed or opened; no performance ran; E, F, A1, S1 and S2 were not constructed; the paired 179-month sample was not created.** `TARGET_X01_OUTCOME_ACCESSED = NO`. **A seal is not run authorization.** use=X01 contract seal. seal=**SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`**; `PREREG_SEALED=YES`; trial contribution **0** - a commit and a seal are not strategy-return attempts and create no `VARIANT_ATTEMPT`. |
| 2026-09-09T20:30:32Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `qros-state.yaml`; `research/extensions/TRIAL_LEDGER.md` | Row 33. class=PURE_MECHANICAL_VERIFICATION. sample=none - **no dataset, panel or results file was read.** session=X01 pre-execution gate session (Claude Opus 5), under Aaron's pre-execution-build authorization. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**nothing.** Owner declarations recorded: `X01_EXECUTION_LANE = FULL` (an explicit owner lane declaration, **not inferred from eligibility**); `SHARED_DATABENTO_HOME_GATE = RESOLVED` by adopting the **existing distributed accounting form** - **no KB registry CSV was created, no KB schema was invented, and commodity-carry-research was not modified**; `TSMOM_TRIAL_LEDGER_CURATOR` = the X01 executing/governance session; `D-ETF-COUNT` historical stays **UNKNOWN** and was **not reconstructed or fabricated**, while the forward convention is `E_COUNTS_AS_ONE_PROSPECTIVE_ATTEMPT` with `E` contributing **+1 on the ETF panel only**, gate `RESOLVED_FOR_X01_EXECUTION`, and effect on the Databento `+3` is **NONE**. **ELIGIBLE != AUTHORIZED**: the runtime now derives `LANE=FULL STAGE=C eligible=9/12`, and no eligible edge authorizes target execution. **NO X01 target series was constructed: E, F, A1, S1 and S2 do not exist; the paired 179-month sample was not created; no Sharpe, dS, bootstrap or crisis statistic was computed; nothing was evaluated and no `VARIANT_ATTEMPT` row was created.** `TARGET_X01_OUTCOME_ACCESSED = NO`. The sealed preregistration is byte-identical at `9c7b104f...96986743` and `SEAL=VERIFIED`. use=X01 pre-execution operational gate. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`; `PREREG_SEALED=YES`; trial contribution **0**. |
| 2026-09-09T20:39:24Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/{x01_runner.py, x01_contract_tests.py, X01_EXECUTION_MANIFEST.json, DEVIATIONS.md}`; `.gitattributes`; `research/extensions/validate_wave0.py` | Row 34. class=PURE_MECHANICAL_VERIFICATION. sample=**the frozen X01 inputs were touched ONLY as opaque bytes for hashing** - the ETF CSV and the three parquet panels were streamed through sha256 and **never parsed into prices or returns**. No other dataset was read. session=X01 pre-execution gate session (Claude Opus 5), under Aaron's Rule-14 build authorization `X01_RUNNER_PRE_EXECUTION_ONLY`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** Built the pre-execution machinery: an execution manifest pinning the sealed contract, the seal revision, 8 tracked-text modules by **git-blob sha256** and 4 git-ignored data files by **raw-file sha256**, plus the cross-repo carry S2 revision and file hash; a runner whose `preflight` verifies every pin and whose `execute` **refuses**; synthetic contract tests; the sealed-6.1 seed-entropy deviation record; and a narrow `.gitattributes`. **Two real defects were found and fixed by this build**: the ETF panel was initially pinned as tracked text when `data/` is git-ignored, and a fresh `git worktree` checkout of the seal-persistence revision materialised CRLF that the QROS state parser refuses - the committed blob was LF and derived `SEAL=VERIFIED`, but the checked-out file did not parse. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed; the paired 179-month sample was NOT created; no Sharpe(E), Sharpe(F), dS, bootstrap or crisis statistic was computed; no target return series was written, generated or read.** The runner imports no dataframe or array library, defines no target-construction entry point, and its `execute` subcommand exits non-zero. All contract tests ran on a hand-built synthetic price path, synthetic position books and synthetic manifests. `TARGET_X01_OUTCOME_ACCESSED = NO`. Building research execution infrastructure is **not** strategy execution: `STRATEGY_BUILD_STARTED = NO`. use=X01 pre-execution operational readiness. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and **not reopened**; trial contribution **0** - a runner build creates no `VARIANT_ATTEMPT`. |
| 2026-09-09T20:58:13Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | runner base `a07edec107743c76538ea9e1d546f5b73b51689b`; `research/extensions/x01/X01_EXECUTION_MANIFEST.json`; `.gitattributes` | Row 35. class=PURE_MECHANICAL_VERIFICATION. sample=**frozen inputs touched ONLY as opaque bytes for hashing** - the ETF CSV and the parquet panels were streamed through sha256 and **never parsed into prices or returns**. session=X01 runner-base / manifest-binding session (Claude Opus 5), under Aaron's two-commit pre-execution authorization. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** **FUTURES INPUT INVENTORY RESOLVED MECHANICALLY.** Exactly **two** `*_v2.parquet` files exist (`settle_v2`, `oi_v2`) plus `contracts_meta.parquet`; the earlier third-`_v2` report is **incorrect**. Two further parquet exist and are **NOT_CONSUMED_BY_X01**: `settle_panel` and `oi_panel`, the superseded raw-symbol-keyed panels retained as implementation-error lineage, named neither by the sealed contract nor by any accepted implementation. `data/DGS3MO.csv` is also NOT_CONSUMED - a risk-free series would be material only under O-6 Option 2, and Aaron adopted Option 1 SYMMETRIC_ZERO_CARRY. All three material futures inputs plus the ETF panel carry raw-file sha256 pins. **SEED IDENTITY CORRECTED:** `.entropy` cannot identify the three child streams - all three children of `SeedSequence(7)` report `entropy == 7`. The manifest now records `spawn_key` `[0]`/`[1]`/`[2]` plus a 4x uint32 state fingerprint per arm in the sealed order. **No statistical protocol changed**; the sealed prereg was **not** edited and D-X01-1 is preserved. **CRLF DEFECT CLOSED AND RE-VERIFIED**: a fresh worktree of the runner base now materialises `qros-state.yaml` with **0 CR bytes** and QROS derives `SEAL=VERIFIED` from it; the `.gitattributes` rule was widened to named text types after a fresh checkout showed **19 validator checks failing purely on CRLF-vs-raw-sha256** - the validator's own logic was **not** touched. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed; no paired sample, Sharpe, dS, bootstrap or crisis statistic; no target return series written, generated or read.** `TARGET_CONSTRUCTION_IMPLEMENTATION_BUILT = NO` - `execute` remains a fail-closed stub. `TARGET_X01_OUTCOME_ACCESSED = NO`. use=X01 pre-execution binding. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical; trial contribution **0** - commits create no `VARIANT_ATTEMPT`. |
| 2026-09-09T21:19:09Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/{x01_target_construction.py, x01_construction_tests.py, x01_runner.py, X01_EXECUTION_MANIFEST.json}`; `research/extensions/validate_wave0.py` | Row 36. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** Not the ETF CSV, not any parquet panel - not even for hashing, beyond the manifest's existing raw-byte pins. Every computation ran on **hand-built synthetic fixtures**: synthetic daily price frames, synthetic settlement and open-interest panels, synthetic contract-metadata rows and synthetic position books. session=X01 target-construction BUILD session (Claude Opus 5), under Aaron's `AARON_X01_TARGET_CONSTRUCTION_BUILD_AUTHORIZATION = YES`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** Implemented the sealed construction layer: `construct_E` (ETF reference leg, reusing the accepted signal/sizing/aggregation/cost components unchanged), `construct_futures_leg` for A1/S1/S2 (gross from the accepted `tsmom_chain_net_returns`, costs from the sealed section 3.8 identity-keyed traded-quantity expression), `ratio_adjusted_prices` for S1's causal forward adjustment, S2 bound by calling carry's `fixed_calendar_front_series` directly, and `paired_window` implementing the sealed intersection RULE. **STRATEGY_BUILD_STARTED = YES** - strategy-research CODE construction has begun; this does **not** mean any target result exists. **NO REAL SERIES WAS CONSTRUCTED: E, F, A1, S1, S2 and the paired 179-month sample do not exist; no Sharpe(E), Sharpe(F), dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** The construction module contains no Sharpe, bootstrap or CI code and no real data path, performs no work on import, and reads no file at module level - all asserted by test. `TARGET_X01_OUTCOME_ACCESSED = NO`. Defence-in-depth strengthened: pinned tracked text is now compared **twice** - blob-at-HEAD for a later committed edit, and LIVE LF-normalised worktree bytes for an uncommitted one - independently of the dirty-tree policy, which still refuses the same path on its own. **Preflight is deliberately REFUSING (8 conditions)** because the new modules are declared `UNCOMMITTED_PENDING_BINDING` with `target_construction_revision = null`; **no git revision or blob pin was fabricated** and preflight was **not** weakened to go green. **NO COMMIT was taken this turn.** use=X01 target-construction implementation. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and **not reopened**; trial contribution **0** - a build creates no `VARIANT_ATTEMPT`. |
| 2026-09-09T22:05:17Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/{x01_target_construction.py, x01_construction_tests.py, X01_EXECUTION_MANIFEST.json}` | Row 37. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** Not the ETF CSV, not any parquet panel; the only contact with those bytes was the pre-existing manifest and preflight raw-sha256 pins, which stream them without parsing. Every computation ran on **hand-built synthetic fixtures**. session=X01 target-construction BOUNDED FABLE REPAIR session (Claude Opus 5), under Aaron's repair authorization following `X01_TARGET_CONSTRUCTION_IMPLEMENTATION_AUDIT = FAIL`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** Byte-drift check ran FIRST and PASSED: all eleven audited pins matched, so `IMPLEMENTATION_BYTE_DRIFT = NO`; the sealed preregistration is byte-identical at `9c7b104f...96986743` and was **not reopened**. Repaired the three implementation blockers plus four further defects, all in the daily futures book and the S1 adjustment. **BLOCKER 1** the book re-struck an already-shifted held frame and applied it a step later, so the quantity earning month M followed the M-2 decision; the strike now reads the unshifted DECISION frame and the sealed shift appears exactly once. **BLOCKER 2** the strike was bound to the calendar month-end LABEL, which is frequently not a session at all - the sealed first paired month-end 2011-07-31 is a Sunday - so such months never re-struck; the strike now binds to each root's last PRICEABLE session of the decision month, and no row is fabricated for a non-session label. **BLOCKER 3** a root-local missing session dropped the root from the book, creating a phantom exit and re-entry and charging a phantom cost; the last known identity and quantity now carry, and a trade is charged only on a genuine event. **S1** an exhausted fallback continued on the stale factor and emitted a usable-looking adjusted series across a roll that was never priced; the series now TERMINATES at the undefined roll, the undefined span is blanked and dated, and an undefined root is no longer silently averaged over inside the ag basket. **SEALED SECTION 3.5 MISSING-ROOT-MONTH** is now implemented as written - the leg is re-formed from the remaining ELIGIBLE roots, renormalised to the same total leg weight, recorded per month, with the ineligible root's forced exit disclosed rather than vanished. **ONE FURTHER DEFECT WAS FOUND BY THIS REPAIR AND IS DISCLOSED HERE**: F charged a month-end rebalance to the month of the strike while E charges it to the month the new position is held, so the two legs' rebalance costs sat one month apart in a difference-of-Sharpes estimand; a trade is now charged to the period in which the position it creates earns, which is E's own convention. Pre-first-strike months are no longer emitted as zero-return padding. **TEST QUALITY REBUILT AFTER THE AUDIT REJECTED IT.** The source-string checks offered as evidence of economic correctness are DELETED, as is the vacuous isinstance import-purity assertion. The daily book is now checked against closed-form oracles written from the sealed algebra with the multiplier and per-side cost typed as literals so production cannot move the oracle with itself; stage-D timing is proved by a differential perturbation needing no arithmetic at all; the cash-first route and `ROLL_COST_DOUBLE_COUNT = NO` are proved by INTERVENTION on the live ledger object rather than by reading source text; import purity is instrumented mechanically with a live control read. **Every repair was then SABOTAGED and the tests were required to fail: 8 of 8 reintroduced defects were caught**, and the module was byte-restored and re-hashed after each. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe(E), Sharpe(F), dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. commodity-carry-research was **not modified** - its HEAD is unchanged at `f0847d7c5bc04aa2048b64fa37a3e0c51347c2d3` with no tracked change. **Preflight is deliberately still REFUSING (8 conditions)**: the two construction modules remain `UNCOMMITTED_PENDING_BINDING` with `target_construction_revision = null`; **no revision or blob pin was fabricated and preflight was not weakened.** The validator registry needed no change - both modules were already declared - and no validator semantic logic was touched. **NO COMMIT was taken this turn.** use=X01 target-construction defect repair. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and not reopened; trial contribution **0** - a repair creates no `VARIANT_ATTEMPT`. |
| 2026-09-10T07:13:04Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/{x01_target_construction.py, x01_construction_tests.py, X01_EXECUTION_MANIFEST.json}` | Row 38. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** Every computation ran on hand-built synthetic fixtures; the frozen panels were touched only by the pre-existing manifest and preflight raw-sha256 pins, which stream bytes without parsing them. session=X01 root-specific month-end causality repair session (Claude Opus 5), under Aaron's bounded repair authorization following the fresh Fable micro-closure `ROOT_SPECIFIC_MONTH_END_BLOCKER = OPEN`, `BLOCKER_TYPE = TEMPORAL_CAUSALITY_CODE_DEFECT`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** Byte-drift check ran FIRST and PASSED on all four pins, so `IMPLEMENTATION_BYTE_DRIFT = NO`; the sealed preregistration is byte-identical at `9c7b104f...96986743` and was not reopened. **ONE CODE DEFECT, ONE FIX.** The monthly rebalance EVENT was timestamped at the root's own last priceable session, which for a root that stops printing before the panel's month-end precedes the exposure-level decision that determines the event's quantity - recording a transaction earlier than its own cause. The event now carries `panel_last_session[M]`. Two dates are now named separately: the MARK date, the root's last available settle inside the decision month, which prices the sealed quantity under the already-accepted stale-mark convention; and the EVENT date, the panel month-end, which is when the decision exists. The event timestamp is the model's monthly rebalance event and is **not** a claim that the exchange traded that root on that date; every such case is disclosed in `stale_mark_rebalances`. One consequential companion change: applying a strike no longer restates the held identity, because writing the mark date's identity back at a later event date could revert a roll made in between and manufacture a round trip. Under the previous scheduling that assignment was a no-op, so removing it preserves the accepted semantics rather than altering them. **NO SCIENTIFIC CHOICE WAS CREATED.** Signal, decision weights, quantity formula, root price convention, stale-mark convention, monthly gross, first-earning-period semantics, ag-basket construction and E semantics are all untouched. **INVARIANTS VERIFIED MECHANICALLY, not asserted:** running the corner-case fixture under the OLD timestamp and the NEW one leaves monthly gross matching the same independent oracle to 6.9e-18 and leaves the total cash cost bit-identical at 0.002364180322; only the per-month REPORTING placement moves, and it moves onto the already-accepted E/F earning-period convention. The required corner case was added - root A dark on the panel's final two sessions of the month, peer priceable through month end, peer final-session move flipping the exposure decision from +2.000000 to -0.243803 on the REAL signal path - and the mandated sabotage restoring the old root-local timestamp was **CAUGHT**, failing the timestamp and cost-period assertions while the gross and total-cost assertions continued to pass, which is what makes those invariants evidence rather than a claim. Writing this oracle also exposed a defect in **my own test**: it omitted the sealed section 3.2 cents divisor, which cancels out of gross but not out of a cost that is linear in quantity; the oracle was corrected and the divisor is now checked explicitly through the cost channel. All eight previously closed sabotages still bind. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe, dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. Real target panel reachability was **not** inspected. commodity-carry-research was not modified. Preflight still REFUSES on 8 conditions with `target_construction_revision = null`; no revision or blob pin was fabricated and preflight was not weakened. **NO COMMIT was taken this turn**, and the repaired bytes return to the SAME Fable session for one final micro delta closure - this session does **not** self-certify. use=X01 bounded temporal-causality repair. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and not reopened; trial contribution **0**. |
| 2026-09-10T07:48:07Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/x01/{x01_construction_tests.py, X01_EXECUTION_MANIFEST.json}` | Row 39. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** Synthetic fixtures only; the frozen panels were touched solely by the pre-existing manifest and preflight raw-sha256 pins, which stream bytes without parsing them. session=X01 pre-bind TEST CLOSURE session (Claude Opus 5), under Aaron's test-closure-only authorization after same-session Fable returned `ROOT_SPECIFIC_MONTH_END_BLOCKER = CLOSED`, `X01_TARGET_CONSTRUCTION_IMPLEMENTATION_AUDIT = PASS`, `TARGET_CONSTRUCTION_IMPLEMENTATION_ACCEPTED = YES`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** **NO PRODUCTION CODE CHANGE.** `x01_target_construction.py` is byte-identical to the accepted hash `787b27b1...2a2f579e34` before and after this phase, re-verified after every sabotage run. This phase closed a TEST-COVERAGE gap only. Added the missing regression fixture Fable identified: a root whose MARK date precedes the panel EVENT date with a GENUINE roll in between - the front hands over to the successor contract on a session where that successor has no settle, so the identity advances while the mark cannot. Asserted against an independent literal oracle that the delayed rebalance re-sizes the identity actually held (one side), that the intermediate roll is preserved and charged exactly twice, that no reverting pair or return trip is manufactured, and that the following session trades nothing. **TWO DEFECTS IN MY OWN TESTS WERE FOUND AND FIXED, and they are the reason this phase was worth running.** First, the fixture's weight sequence made the position reverse sign between the two months under test, which makes `abs(q_new - q_old)` and `abs(q_old) + abs(q_new)` numerically IDENTICAL - the correct and the reverting counterfactual collapse onto each other and the assertion discriminates nothing. Fixed with a same-sign sequence, and an explicit non-vacuity assertion now guards both counterfactual pairs so this cannot silently recur. Second, `STALE_IDENTITY_NOT_RESTORED` was written as an inequality against ONE reverting form and still PASSED under the sabotage, because restoring the stale identity produces the reverting pair PLUS a return trip, not the reverting pair alone; the flag now carries the positive equality, which is what actually fails. One assertion label claiming to isolate the peer exposure was relabelled to what it really checks, a June total. Mandated sabotage: **CAUGHT**. Restoring the obsolete mark-date identity at the delayed event fails 6 checks. A second mutation freezing the identity so the intermediate roll never happens was added, because without it two of the six required flags would have been free-riding on a mutation that never touches the roll; across the two mutations **all six flags bind**. All eight previously closed sabotages still bind. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe, dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. commodity-carry-research was not modified. Preflight still REFUSES on 8 conditions with `target_construction_revision = null`; **nothing was bound and no pin was fabricated.** **NO COMMIT was taken this turn**, and the bytes return to the SAME Fable session for a test-only micro closure - this session does not self-certify. use=X01 pre-bind test closure. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and not reopened; trial contribution **0**. |
| 2026-09-10T08:16:17Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | freeze revision `851e9d3fd8d23c2f6802f796fcc32a21c5bd5a70`; binding revision `c6f4ea127b3af91cd0ab541913d8b1ce4b50ab8e`; `research/extensions/x01/X01_EXECUTION_MANIFEST.json` | Row 40. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** The frozen ETF CSV and parquet panels were touched only as opaque bytes by the manifest's raw-sha256 pins; nothing was parsed into prices or returns. session=X01 target-construction FREEZE/BIND session (Claude Opus 5), under Aaron's `FREEZE/BIND X01 TARGET-CONSTRUCTION IMPLEMENTATION` authorization, following same-session Fable's final closure: `IDENTITY_REVERT_TEST_GAP = CLOSED`, `PRE_BIND_TEST_CLOSURE = PASS`, `X01_TARGET_CONSTRUCTION_IMPLEMENTATION_AUDIT = PASS`, `IMPLEMENTATION_BLOCKERS = 0`, `TARGET_CONSTRUCTION_IMPLEMENTATION_ACCEPTED = YES`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** All six pre-freeze pins matched, so there was no `ACCEPTED_IMPLEMENTATION_BYTE_DRIFT`; the sealed preregistration is byte-identical at `9c7b104f...96986743` and was not reopened. **THE IMPLEMENTATION IS NOW FROZEN AND BOUND.** The freeze revision is `851e9d3fd8d23c2f6802f796fcc32a21c5bd5a70`, and the blobs committed there are `787b27b1...2a2f579e34` for the construction layer and `0e3248ab...df6216f27` for its synthetic test module - **byte-identical to what the independent audit accepted, with no cleanup, refactor, reformatting or comment rewrite before freezing.** The binding revision is `c6f4ea127b3af91cd0ab541913d8b1ce4b50ab8e`, which makes the runner bind-aware, and the manifest commit records the pins. **THREE commits, and the reason is mechanical, not stylistic.** The manifest must pin the FINAL bytes of every pinned path, so it cannot be built until those bytes are committed; and the accepted test module had to ENTER the freeze commit at its accepted hash while two of its assertions described the PRE-binding state and therefore had to change once binding happened. Those two requirements cannot be satisfied in one commit, and collapsing them would have produced either a stale pin or a frozen artifact that is not the reviewed one. The intermediate revision is deliberately not independently green; the freeze revision and the final state both are. **BINDING IS NOT AUTHORIZATION, and this is enforced structurally rather than by convention:** `execute` refuses unconditionally and consults neither preflight, nor the manifest, nor the binding block, so a passing preflight cannot make it run. `execution_authorized` remains **false**. Preflight gained a binding check that reads git rather than the manifest's own word: the frozen blob must reproduce the bytes the independent audit accepted, and a forged accepted pin, a null revision, or any module returned to pending each still REFUSE - all three exercised by test. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe(E), Sharpe(F), dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. commodity-carry-research was not modified. Nothing was pushed; no PR, no merge, no amend; the branch remains local. use=X01 target-construction freeze and mechanical binding. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED` re-derived after the freeze commit, sealed bytes byte-identical and not reopened; trial contribution **0** - freezing and binding code creates no `VARIANT_ATTEMPT`. |
| 2026-09-10T08:25:27Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `qros-state.yaml` (`workflow_stage` C to D; header; `session_log`) | Row 41. class=PURE_MECHANICAL_VERIFICATION. sample=**none - no dataset, panel or results file was read.** session=X01 C-to-D workflow-stage transition session (Claude Opus 5), under Aaron's `QROS C -> D TRANSITION` authorization for `TSMOM-EXT-001`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**nothing.** **THE PROJECT IS NOW AT STAGE D.** Per QROS section 3 the stages mean C = Build (pipeline, strategy, statistics, tests) and D = Mechanical verify (invariants, leakage, timestamps, train/test separation, accounting identities, deterministic reproduction). The runtime reported `C -> D = ELIGIBLE` immediately before the edit and `LANE=FULL STAGE=D` immediately after, with state-file legality, partition discipline and packet-record integrity reporting no rejection. **ELIGIBLE IS NOT AUTHORIZATION** - the runtime says so itself, and the authorization here is Aaron's. The mutation was the minimum the convention allows: the DECLARED `workflow_stage` field, Aaron's authorization recorded verbatim in the state file header, and one appended `session_log` entry. `human_decisions[]` was deliberately NOT touched - section 2.9 closes its type vocabulary at DORMANT_ENTER, DORMANT_EXIT and REVIEW_ATTEMPT_EXCLUDED, and inventing a fourth token would be a schema violation dressed as diligence. Lane, prereg pointer, seal_revision, materiality, outcome_exposure, inputs, ops_events and packets are unchanged. **D IS NOT EVIDENCE.** QROS section 3 puts *run the preregistered study* at stage **E**, so nothing here produced or opened a target outcome. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe(E), Sharpe(F), dS, bootstrap, CI or crisis statistic was computed; no target return series was written, generated or read.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. `execution_authorized` remains **false** and `execute` still exits non-zero without consulting preflight, the manifest or the binding block. Verified before and after: `SEAL=VERIFIED`; the implementation is frozen at `851e9d3fd8d23c2f6802f796fcc32a21c5bd5a70` and bound, every binding check reading git rather than the manifest's own word; `IMPLEMENTATION_BLOCKERS = 0`; construction tests, contract tests and the 127-check validator all PASS. **Recorded consequence, not hidden:** section 2.6(c) places every edge whose tail stage is at or after D in the OUTCOME-BEARING SET, so the next edge `D -> E` is one at which an outcome may be generated - which is exactly why it needs its own Aaron authorization and did not get one here. **Deferred, not repaired, per Aaron:** the repository-wide CRLF-in-worktree condition behind `WORKTREE_BYTES_DIFFER_FROM_HEAD`. It holds `B -> C` and `I -> J` only, touches no X01 pinned input, and the runtime does not report it against `C -> D`. use=authorized QROS workflow-stage transition. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, sealed bytes byte-identical and not reopened; trial contribution **0** - a stage transition is not a strategy-return attempt and creates no `VARIANT_ATTEMPT`. |
| 2026-09-10T08:39:07Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `qros-state.yaml` (`workflow_stage` D back to C; header; `session_log`) | Row 42. class=PURE_MECHANICAL_VERIFICATION. sample=**none - no dataset, panel or results file was read.** session=X01 stage-correction session (Claude Opus 5), under Aaron's `PREMATURE_STAGE_DECLARATION_CORRECTED_AFTER_BUILD_COMPLETENESS_CHECK` authorization. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**nothing.** **THE STAGE DECLARATION IS CORRECTED FROM D BACK TO C, and the correction is mine to own.** Row 41 recorded a C to D transition earlier the same day. That transition was mechanically legal and it was still premature: the runtime evaluates NO build-completeness precondition for `C -> D`, so ELIGIBLE could not mean the build was finished, and the producing session did not check that separately before declaring D. A pre-evidence completeness check against the sealed contract then established `SEALED_INFERENCE_IMPLEMENTATION = ABSENT` - twelve concrete missing items, nine of them sealed computations: Sharpe(E) and Sharpe(F) on the constructed streams, dS, the paired joint stationary bootstrap, invalid-replicate handling with the 9,500 floor, the 95 percent percentile CI, the classification against -B, the S1/S2 descriptive arms, the sealed crisis windows, and the section 7 path diagnostics. `UNSEALED_DOF = NO`: this is missing code, not a missing Owner choice. Per QROS section 3, C is Build - pipeline, strategy, statistics, tests - so C is the truthful stage while the statistics half is unbuilt. **WHAT IS NOT WITHDRAWN:** the construction implementation remains independently accepted, frozen at `851e9d3fd8d23c2f6802f796fcc32a21c5bd5a70` and bound, every binding check still passing; the seal is untouched and still derives VERIFIED; no preregistration byte was changed. This is not a methodology change, a hypothesis change, a prereg amendment, a scientific reset, or a reopening of construction. No runtime transition edge was invented - the runtime specifies no `D -> C` edge, and `workflow_stage` is a DECLARED self-report the state file itself calls the session's to state and Aaron's to correct. **NO TARGET EXECUTION: no E, F, A1, S1 or S2 series exists on real data; no Sharpe, dS, bootstrap, CI or crisis statistic was computed or opened; execution_authorized remains false and `execute` still exits non-zero.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. use=authoritative stage-declaration correction. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, sealed bytes byte-identical and not reopened; trial contribution **0**. |
| 2026-09-10T13:00:31Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | freeze revision `bc6c80536cd0fefc2ed1f440ca65d1c73d270e37`; binding revision `52cd5b4108ff4a9d6247e1641df8f74b14cfc4d1`; `research/extensions/x01/X01_EXECUTION_MANIFEST.json` | Row 43. class=PURE_MECHANICAL_VERIFICATION. sample=**NO target data was read at all this phase.** The frozen panels were touched only by the manifest's existing raw-sha256 pins, which stream bytes without parsing them. session=X01 inference + construction-delta FREEZE/BIND session (Claude Opus 5), under Aaron's `FREEZE/BIND X01 INFERENCE + CONSTRUCTION DIAGNOSTIC INTERFACE DELTA` authorization, following same-session Astra's final closure: `X01_INFERENCE_IMPLEMENTATION_AUDIT = PASS`, `X01_CONSTRUCTION_INTERFACE_DELTA_AUDIT = PASS`, `SEALED_ITEMS_1_TO_9_ACCEPTED = YES`, `IMPLEMENTATION_BLOCKERS = 0`, `UNSEALED_DOF = NO`. **ts is an observed clock read taken immediately before this append and is a LOWER BOUND on it.** revealed=**no X01 target quantity.** All nine accepted pins matched before any commit, so there was no `X01_FREEZE_BIND_BYTE_DRIFT`; the sealed preregistration is byte-identical at `9c7b104f...96986743` and was not reopened. **BOTH ACCEPTED HALVES ARE NOW FROZEN AND BOUND.** The freeze revision is `bc6c80536cd0fefc2ed1f440ca65d1c73d270e37` and the committed blobs there are, verified from git rather than from the worktree: `1ece832e...5ec50b06` for the sealed inference layer, `0437f9cd...72c4c65c` for its tests, `262aac9a...e412eb83` for the construction layer with the additive section 7 diagnostic interface, and `4e5cc510...7245a90b` for its tests - byte-identical to what the independent audit accepted, with no cleanup, refactor or reformatting before freezing. The binding revision is `52cd5b4108ff4a9d6247e1641df8f74b14cfc4d1`. **THE SEQUENCE WAS DERIVED FROM THE CURRENT CONTRACT, NOT COPIED FROM THE PREVIOUS FREEZE.** Three commits again, for the same two mechanical reasons: the runner cannot name a freeze revision that does not yet exist, and the manifest must pin the FINAL bytes of every pinned path, so it can only be built after those are committed. The middle commit also had to carry the two test modules whose assertions described the PRE-binding state, which by construction cannot be frozen already asserting a binding that does not exist. **HISTORICAL PROVENANCE IS PRESERVED, NOT OVERWRITTEN.** The first construction-only freeze `851e9d3f` with pins `787b27b1` and `0e3248ab` is recorded in `superseded_freezes` together with what superseded it and the note that those blobs remain exactly those bytes at that revision. The old hash no longer reads as current. Binding status is DERIVED from two separate questions - did the freeze reproduce the REVIEWED bytes, and does the worktree match what is COMMITTED - with `post_freeze_updated` disclosing which bound files have legitimately moved since their own freeze; it currently names the construction test module, for exactly the reason above. Preflight verifies both halves with one rule, and a forged accepted pin, a null revision or anything returned to pending each still REFUSE. **BINDING IS NOT AUTHORIZATION, enforced structurally:** `execute` refuses unconditionally and consults neither preflight, nor the manifest, nor either binding block, so a passing preflight cannot make it run. `execution_authorized` remains **false**. **NO TARGET EXECUTION: E, F, A1, S1 and S2 were NOT constructed on real data; the paired 179-month sample does not exist; no Sharpe(E), Sharpe(F), dS, bootstrap, CI or crisis statistic was computed; no evidence artifact exists.** `TARGET_X01_OUTCOME_ACCESSED = NO`. `PROTECTED_FORWARD_DATA_OPENED = NO`. The workflow stage was **not** advanced and remains **C**; Stage D to E was not approached. commodity-carry-research was not modified. Nothing was pushed; no PR, no merge, no amend; the branch remains local. Remaining known Stage-C build items are unchanged and unstarted: the evidence artifact schema, production orchestration, and the machine-readable Owner execution-authorization mechanism. use=X01 inference and construction-delta freeze and binding. seal=SEALED at `df5b28ab7324c7ba789ab231431f077288c3fd84`, `SEAL=VERIFIED`, sealed bytes byte-identical and not reopened; trial contribution **0** - freezing and binding code creates no `VARIANT_ATTEMPT`. |
| 2026-09-12T20:08:01Z | CURRENT_REVIEW_SCOPE | GENERATED_NOT_SEEN | NONE | ops/execution-journal/X01-RUN-0001.step2.json | X01 pre-execution step 2. Prospective exposure and trial commitment recorded BEFORE any target construction. run_id=X01-RUN-0001. authorization=X01-RUN-0001. Databento contribution +3 for A1, S1 and S2; E counts as one prospective ETF attempt (E_COUNTS_AS_ONE_PROSPECTIVE_ATTEMPT). Bootstrap replicates and crisis-window slices are NOT attempts. Machine record: ops/execution-journal/X01-RUN-0001.step2.json. |
| 2026-09-12T20:14:37Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | `research/extensions/x01/X01_EVIDENCE.json` sha256 `8a98af94c6eeb3067b041e53221d713c4281f8a0dd7e97eb00ac083046141d4f` | Row 45. class=TARGET_PERFORMANCE_EXPOSURE. The X01 first-execution classification transitions GENERATED_NOT_SEEN to REVEALED_TARGET_METRIC under Aaron's explicit Owner decision AUTHORIZE REVEAL OF X01-RUN-0001 AND ENTER S4 VERDICT. run_id=X01-RUN-0001. authorization_id=X01-AUTH-0001. revealed=the sealed X01 primary and secondary evidence in the artifact named above - Sharpe(F), Sharpe(E), the primary scalar and its 95 percent interval, and the preregistered descriptive secondaries. session=Claude Opus 5 S4 deterministic extraction session. NO recomputation and NO new execution: the already-generated sealed artifact is read, the production runner is not invoked, and no real source panel is parsed. This reveal creates NO new VARIANT_ATTEMPT - the four attempt rows were committed at step 2 of X01-RUN-0001 and a reveal of an existing result is not a new trial. The authorization X01-AUTH-0001 was already CONSUMED by that run and is not re-spent here. |
| 2026-09-13T16:56:24Z–16:57:01Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | S_0 seal-time snapshot `data/prospective/S0_20260913T165624Z.csv` sha256 `c4a21dc86038f9f0d06e32a267810b46cb39d9dd3549c35ef3329c87f884d6fb` (3,323,945 bytes); identity `data/prospective/S0_20260913T165624Z.identity.json`; overlap record `data/prospective/S0_20260913T165624Z.overlap.json`; frozen panel `data/close_prices_raw.csv` sha256 `3d2a7a56dbd92d4ff8138cfd894c87f5ac5ac088a11165db870673e0c05c3c31` re-verified UNCHANGED before and after | Row 46. class=PURE_MECHANICAL_VERIFICATION. sample=`dataset.yfinance.multi-asset-etf-panel`, a NEW snapshot, never an overwrite (LOCKBOX §3.1). session=Claude Opus 5, C-A S1 final seal-gate preflight, under Aaron's `FINAL S1 SEAL GATE` instruction to complete CA §Z.1 item 7. **fetched**=all 30 universe tickers, `yfinance Ticker.history(period='max', auto_adjust=True)`, 1993-01-29..2026-09-11, 8,462 rows × 30 cols. **historical values changed?** YES at the adjusted-PRICE level and NO materially at the daily-RETURN level: over the 8,400 common trading days (calendar identical — zero days present on only one side) the per-ticker adjusted-close ratio is a near-constant per-ticker factor (1.0000–1.0150) and the max abs delta daily return across the canonical 17 is 3.389e-06 (TLT, 2005-07-05); USO, UNG, GLD and FXY — the four canonical objects that pay no distribution — agree **bit-identically**, which is the signature of routine dividend back-adjustment rather than a vendor correction. **revealed**=vendor prices and their per-ticker daily returns only. **No signal, no volatility, no position, no portfolio return, no Sharpe, no drawdown and no strategy quantity of any kind was computed**, so research-axis exposure is unchanged. 62 rows fall after the frozen boundary (2026-06-15..2026-09-11) and are POST_SEAL_UNSCORED_STATE_INPUT_ONLY under CA §E.1 — not scored, not T3, not T4. **Ordering disclosure (LOCKBOX §3.4):** this row was written AFTER the fetch and the overlap comparison, not before. The requirement is disclosed rather than papered over; because the classification is `NO_OUTCOME` the normalized exposure for every scope is unmoved either way. use=CA §Z.1 item 7 seal provenance. seal=none; C-A is NOT sealed; `S1_SEALED=NO`. adjudication=the daily-return overlap has NO declared acceptance tolerance in the sealed contract, so it is returned to Aaron as PENDING_CLASSIFICATION PC-1 and is NOT classified by this session. |
| 2026-09-14T00:00:00Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | §J locked-vs-recomputed position diagnostic; record `data/prospective/S0_20260913T165624Z.position_diagnostic.json`; frozen panel `data/close_prices_raw.csv` sha256 `3d2a7a56dbd92d4ff8138cfd894c87f5ac5ac088a11165db870673e0c05c3c31` re-verified UNCHANGED before and after | Row 47, which COMPLETES the adjudication Row 46 left open. class=PURE_MECHANICAL_VERIFICATION. session=Claude Opus 5, C-A pre-seal blocker closure, under Aaron's `PHASE_B_CA_PRESEAL_BLOCKER_CLOSURE` instruction to run ONLY the diagnostic the contract already specifies. **what was run**=the canonical engine on the canonical 17, on BOTH the frozen panel and S_0, each truncated to the frozen historical window ending 2026-06-12 BEFORE any computation; the script asserts zero post-boundary rows entered through prices, volatility windows or momentum lookbacks. **emitted**=only the two quantities §J names for its split/dividend back-adjustment row: max abs delta position = 1.348102e-04 against that row's own 0.01, and 0 sign flips, over 402 decision months and 6,477 position cells. **NOT computed and NOT emitted**=any monthly return, Sharpe, PnL, drawdown, performance statistic, performance proxy, or any position vector; and nothing whatsoever after 2026-06-12. Research-axis exposure is therefore unmoved. **result**=both §J conditions met, so the S_0-versus-frozen overlap is classified MECHANICAL_CORRECTION (no action) and PENDING_CLASSIFICATION PC-1 is CLOSED from existing authority. **No new tolerance, rule or evidence standard was created.** use=CA §Z.1 items 7 and 8. seal=none; C-A is NOT sealed; `S1_SEALED=NO`. |
| 2026-09-14T08:04:09Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | TSMOM-VRP-01 S1 seal package: `research/extensions/vrp/VRP_PREREGISTRATION.md` sha256 `dd5822440bedbe58f49940651bddf656f4dbb593295b59c4eff2b45b89cf53e6`; `VRP_IMPLEMENTATION_ACCEPTANCE_CONTRACT.md` sha256 `4fad50df6ef0c031cdb67e8100034718f49382ce0dd4e6df8e93f29afecb7dbb`; `VRP_EXPOSURE_DISCLOSURE.md` sha256 `afae108d6e894c3822c783d45d77b85e09838578e5e7bf8040ad6dac6a3788c1`; seal commit `16d84545ba1385a482dbac7e776b31275f6fa5f7` | Row 48. class=PURE_MECHANICAL_VERIFICATION. sample=**none read**. session=Claude Fable 5.1, TSMOM-VRP-01 S1 DESIGN + SEAL, under Aaron's explicit S1 authorisation. **Appended retrospectively at S2 acceptance** under the row drafted in `VRP_EXPOSURE_DISCLOSURE.md` §5 (S1 drafted it; the accepted S2 session appends it, as that document directs). revealed=**nothing**. No historical VIX-futures settlement, price or index-return history, no candidate-return series, no carry statistic and no Stage-A or Stage-B outcome was read, downloaded, computed or inspected; no VIX-futures file existed in the repository; no external page was opened; no canonical quantity was computed for any month; the protected C-A store, key, position and return layers were not touched. design-exposed episode list attached by reference to `research/extensions/vrp/VRP_EXPOSURE_DISCLOSURE.md` §3 (GFC, 2010, Feb 2018, Mar 2020, CY2022, Aug 2024, Apr 2025) — recorded so it can never later be presented as fresh confirmation. use=the sealed TSMOM-VRP-01 contract. seal=`VRP_SEAL_MANIFEST.md`; `PREREG_SEALED=YES`. |
| 2026-09-14T16:22Z–17:05Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/vrp/VRP_DATA_MANIFEST.md`; `VRP_SPECIFICATION_REGISTRY.md`; `VRP_NORMALIZED_SCHEMA.md`; `VRP_S2_ACCEPTANCE_REPORT.md`; `s2/VRP_S2_ACCEPTANCE_LOG.json`; `s2/VRP_CA_ACCESS_AUDIT.json`; S2 build commit `72499019ce9ef97f16d1ddf3b3982697a4e6c4cc` | Row 49. class=PURE_MECHANICAL_VERIFICATION. sample=**`dataset.cboe.vix-futures-monthly-chain`** (new; `SAMPLE_REUSE.md` KB-6), raw contract settlements, **mechanical use only**. session=Claude Opus 5, TSMOM-VRP-01 S2 implementation Main Agent, under Aaron's `PHASE D — S2 BUILD + DATA ACQUISITION` authorisation. revealed=**acquisition and construction mechanics only** — 274 Cboe official raw per-contract files SHA-256 pinned; 268 monthly contracts; date range 2004-03-26→2026-09-11; 47,160 rows (46,331 settled); 0 duplicates; contract-identity and expiry identity (264/264 expired contracts reproduced by the dated expiry-rule registry); empirical CFE calendar with 0 unexplained differences and 3 declared CFE-only sessions; roll-weight identities on 5,617 chain days; the 2007 quotation/multiplier normalisation; §F.4 missing-settlement flags (0 in the frozen window); §F.6 first eligible complete month = 2006-09, giving 240 Stage-A months. **NO return, basis, carry, roll yield, average price, cumulative return, Sharpe, CAGR, drawdown, bootstrap interval, tail statistic, crisis-window outcome or cost-adjusted performance was computed on the real chain.** Stage A and Stage B were implemented and exercised on SYNTHETIC fixtures only; the governed-run gate refuses real data and no Owner execution authorisation exists. **No trial was spent** (`STAGE_A_TRIAL_SPENT = NO`, `STAGE_B_TRIAL_SPENT = NO`). C-A exposure: **NONE** — static audit over 21 package files, no C-A import, path, key or store, no canonical quantity computed. use=the S2 implementation and its acceptance evidence. seal=`VRP_SEAL_MANIFEST.md`; `PREREG_SEALED=YES`; `REAL_RUN_AUTHORIZED=NO`. |
| 2026-09-14T13:24:08Z | CURRENT_REVIEW_SCOPE | GENERATED_NOT_SEEN | NONE | protected result `data/vrp_protected/TSMOM-VRP-01_stage_a_historical_20260914T132408Z.json` sha256 `baa0a07d647b4d1be082a569022dbcc09d1eb23e5f64b76374449a8139fce1bc` (git-ignored); `research/extensions/vrp/s3/VRP_STAGE_A_RUN_MANIFEST.json`; grant `VRP-AUTH-0001` | Row 50. class=TARGET_PERFORMANCE_EXPOSURE, **generated but NOT seen at this row**. sample=`dataset.cboe.vix-futures-monthly-chain`, context `DESIGN_INFORMED_FIRST_LOCAL_USE`. session=Claude Opus 5, TSMOM-VRP-01 S3 implementation Main Agent, under CHATGPT_FINAL_S2_ACCEPTANCE_ON_BEHALF_OF_OWNER_WORKFLOW relayed by Aaron. run_id=VRP-STAGE-A-RUN-0001, authorization_id=VRP-AUTH-0001, scope ONE_SHOT. generated=the real governed Stage-A monthly excess-return series over 2006-09..2026-08 (240 months) and its sealed stationary-bootstrap interval (10,000 replicates, 10,000 valid, floor 9,500), plus the predeclared descriptives R1-R5 and R12-R14. **Nothing was read at this row**: the package went straight to the VRP family's own protected store as GENERATED_NOT_SEEN and the run command printed integrity metadata only. **This is the event that spends the governed trial**: VIX-chain `N_trials` 0 -> 1, `STAGE_A_TRIAL_SPENT = YES`. Stage B NOT executed (`stage_b_authorized = false` on the grant; the run gate is stage-scoped). C-A exposure NONE. seal=`VRP_SEAL_MANIFEST.md` + `VRP_S1_MECHANICAL_ERRATUM_01.md`; `PREREG_SEALED=YES`. |
| 2026-09-14T13:31Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | `research/extensions/vrp/s3/VRP_STAGE_A_REVEALED_PACKAGE.json`; `research/extensions/vrp/s3/VRP_STAGE_A_RUN_RECORD.md` §2; grant `VRP-AUTH-0002` | Row 51. class=TARGET_PERFORMANCE_EXPOSURE. sample=`dataset.cboe.vix-futures-monthly-chain`. session=same S3 seat (Claude Opus 5). **The single authorised reveal**, under the separate single-use grant `VRP-AUTH-0002`, committed only after the post-compute integrity gate passed 19/19. reveal_count=1. revealed=the predeclared Stage-A evidence package — the annualised arithmetic mean net excess return on committed capital, its 95 % percentile interval, the sealed state and failure class, and the predeclared descriptives R1-R5 and R12-R14 (`PROMOTION_POWER = NONE`). The figures live in the artifacts named above and are not restated here. state=**UNRESOLVED / Class 3 INSUFFICIENT_EVIDENCE-LOW_POWER**; `RESEARCH_STATUS = unresolved`. Under the sealed §O stop rule a Class-3 Stage A means the historical study STOPS and **Stage B never runs** on it; the only sealed continuation is VRP-A-PROSPECTIVE, which was NOT started. Stage-B descriptives R6-R11 were neither computed nor revealed. The Main Agent seat is now outcome-exposed for TSMOM-VRP-01 Stage A (`ops/REVIEWER_EXPOSURE_LOG.md` S30). C-A exposure NONE. seal=`PREREG_SEALED=YES`. |
| 2026-09-14T19:20Z | CURRENT_REVIEW_SCOPE | NO_OUTCOME | NONE | `research/extensions/vrp/VRP_HISTORICAL_CLOSURE.md`; `research/extensions/vrp/diagnostics/VRP_DIAGNOSTIC_DEFECT_001.md`; ops LIFECYCLE records consuming VRP-AUTH-0001 and VRP-AUTH-0002 | Row 52. class=PURE_MECHANICAL_VERIFICATION. session=Claude Opus 5, TSMOM-VRP-01 historical closure, under Aaron's decision NOT to activate VRP-A-PROSPECTIVE. revealed=**nothing new**. The lineage is CLOSED at the already-revealed Stage-A verdict UNRESOLVED / Class 3; that verdict is unchanged. Prospective confirmation is NOT_ACTIVATED_BY_OWNER_DECISION and spends no trial: declining to start a clock constructs no return series. No scheduler, daemon, runner or prospective store existed or was removed; no accepted historical artifact was deleted and the S1 seal was not rewritten. A SEPARATE exploratory lineage VRP-PORTFOLIO-DIAGNOSTIC-01 began and **stopped on VRP-DIAG-DEFECT-001** before producing any portfolio metric: the Stage-B book ledger resets sleeve holdings each calendar month, violating the binding §H.4 identity. **No sealed result is affected** (confirmatory Stage B never ran) and **no new outcome was generated or revealed**; the diagnostic reused the already-revealed Stage-A series as an input, `reveal_count` stays 1. C-A exposure NONE. seal=`PREREG_SEALED=YES`. |
| 2026-09-14T20:40Z | CURRENT_REVIEW_SCOPE | REVEALED_TARGET_METRIC | TARGET_METRIC | `research/extensions/vrp/diagnostics/VRP_PORTFOLIO_DIAGNOSTIC_01.md` and `.json`; `research/extensions/vrp/VRP_FINAL_HANDOFF.md` | Row 53. class=TARGET_PERFORMANCE_EXPOSURE. **SEPARATE EXPLORATORY LINEAGE** VRP-PORTFOLIO-DIAGNOSTIC-01, `PROMOTION_POWER = NONE`. session=Claude Opus 5, under Aaron's bounded repair + diagnostic authorisation. sample=the frozen ETF panel (further reuse, T0) paired with the already-revealed VRP Stage-A series over the 217-month overlap 2008-05..2026-05. revealed=fixed 80/20 portfolio statistics: CORE / VRP / COMBINED annualised mean, vol, Sharpe, max drawdown, worst and best month, positive-month fraction; correlation -0.105; DELTA_SHARPE +0.1573 with paired-bootstrap CI [-0.0052, +0.3113]; SPY-bottom-decile means and D_diag -0.0178; six declared crisis windows; a cost diagnostic. **NO new governed trial**: no strategy-return series with a selection opportunity was constructed - the VRP leg is the already-spent sealed series reused as an input and the core leg is the frozen canonical stream. `STAGE_A_TRIAL_SPENT` stays YES (unchanged, already spent), `STAGE_B_TRIAL_SPENT` stays NO. Confirmatory Stage B was NOT run and remains barred. PRACTICAL_CLASSIFICATION = NOT_COMPELLING. **TSMOM-VRP-01 remains UNRESOLVED / Class 3; this row changes no verdict.** Also records the repair of VRP-DIAG-DEFECT-001 and the revalidation of acceptance items 13-15 with a cross-month fixture. C-A exposure NONE. |
